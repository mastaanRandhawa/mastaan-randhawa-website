# Static assets (copied to site root when building)

Put here anything that should be served as-is at the root of the site:

- **`imgs/`** – e.g. `favicon.webp` (referenced in `index.html` as `imgs/favicon.webp`)
- **`ResumeMay2025Tech.pdf`** – so the “Download Resume” link works

See **DEPLOY.md** for full deployment steps.
